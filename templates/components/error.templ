package components

templ Error(errorMessage string) {
    <div class="flex flex-col items-center gap-3 h-4/5 w-2/5">
        <p class="text-red-500 text-2xl text-center">{errorMessage}</p>
    </div>
}